.maxwidth
  %h1 All Articles

  - if @articles.present?
    - @articles.each do |p|
      = link_to "/articles/" << p.title, "/" << p.slug do
        %span p.title

  .space
  = link_to "Add New Article", new_article_path, class: "button"

